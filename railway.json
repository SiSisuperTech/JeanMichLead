{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn lead_qualifier_full:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
  }
}
